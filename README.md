# Cloud Garden ASCII Diagram

Three.js / Takram cloud visualization with a shader-based ASCII ground effect.

## Run locally

```bash
npm install
npm run dev
```

Open the local URL printed by Vite.

## Build

```bash
npm run build
```

The production files are generated in `dist/`.
